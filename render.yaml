services:
  - type: static
    name: react-expense-tracker
    env: node
    region: oregon
    plan: free
    branch: main
    rootDir: frontend
    buildCommand: npm install --legacy-peer-deps && npm run build
    publishPath: build
    autoDeploy: true
